#!/usr/bin/env bash

BOX=$(uname)

if [[ "$BOX" == "FreeBSD" ]] ; then
  ansible-playbook --ask-become-pass -c local -v /usr/local/etc/ansible/site.yml
else
  ansible-playbook --ask-become-pass -c local -v /etc/ansible/site.yml
fi
